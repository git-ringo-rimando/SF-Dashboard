services:
  - type: web
    name: sf-dashboard
    runtime: docker
    plan: free
    envVars:
      - key: CHROMIUM_PATH
        value: /usr/bin/chromium
      - key: DATA_DIR
        value: /tmp/sf-data
      - key: NODE_ENV
        value: production
